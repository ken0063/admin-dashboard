import type { NextPage } from "next";

const Home: NextPage = (): JSX.Element => {
  return (
    <div className="flex items-center justify-center w-screen h-screen text-3xl font-bold text-teal-400 underline">
      Hello World
    </div>
  );
};

export default Home;
